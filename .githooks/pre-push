#!/bin/sh
echo "Running pre-push checks (verify)..."
npm run verify
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "Push blocked: verify failed."
  exit 1
fi
echo "Verify passed. Proceeding with push."
exit 0