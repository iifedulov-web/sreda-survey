#!/bin/sh
echo "Running pre-push checks..."
npm run test:e2e
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "? Push blocked: e2e tests failed."
  exit 1
fi
echo "? E2E passed. Proceeding with push."
exit 0